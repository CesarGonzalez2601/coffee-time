# coffee-time
Coffee Time is a Kotlin-based café management application designed to handle users, products, orders, payments, inventory, reports, and cash closing. This stage runs through a console interface, uses SQLite for data persistence, and applies OOP, validations, exception handling, and role-based access control.
